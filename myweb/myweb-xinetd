service http
{
       socket_type             = stream
       wait                    = no
       user                    = root
       server                  = /usr/local/bin/myweb 
       server_args	       = arg1
}

